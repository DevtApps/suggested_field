name: suggested_field
description: Implementação simples de um TextField com sugestões para pesquisas simples ou mais complexas, suporta loading para sugestões assíncronas
version: 0.0.3
homepage: https://github.com/DevtApps/suggested_field

environment:
  sdk: '>=3.0.6 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
